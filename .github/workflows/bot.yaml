name: Deploy Telegram Bot

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 9 * * *'  # Ежедневно в 9 утра
  workflow_dispatch:  # Ручной запуск

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        pip install python-telegram-bot pandas openpyxl
        
    - name: Run bot
      run: |
        # Запускаем бота в фоновом режиме на 30 секунд для тестирования
        timeout 30s python bot.py || true
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        
    - name: Create sample files
      run: |
        # Создаем CSV файл ПОСЛЕ работы бота
        echo "id,name,date,status" > applications.csv
        echo "1,Test Application,2024-01-01,completed" >> applications.csv
        echo "2,Another App,2024-01-02,pending" >> applications.csv
        
        # Создаем Excel файл ПОСЛЕ работы бота
        python -c "
import pandas as pd
from datetime import datetime
df = pd.DataFrame({
    'id': [1, 2, 3],
    'name': ['Test App', 'Another App', 'Third App'],
    'date': [datetime.now().strftime('%Y-%m-%d')] * 3,
    'status': ['completed', 'pending', 'in_progress']
})
df.to_excel('applications.xlsx', index=False)
print('✅ Excel файл создан успешно')
"
        
    - name: Upload applications data
      uses: actions/upload-artifact@v4
      with:
        name: applications-data
        path: |
          applications.csv
          applications.xlsx
        retention-days: 30
        
    - name: Notify success
      if: success()
      run: |
        echo "✅ Бот успешно запущен и файлы созданы"
        echo "📊 Содержимое CSV:"
        cat applications.csv
